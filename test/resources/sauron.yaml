apiVersion: v1
kind: Pod
metadata:
  name: sauron
  namespace: barad-dur
spec:
  containers:
    - name: web
      image: nginx
      ports:
        - name: web
          containerPort: 80
          protocol: TCP